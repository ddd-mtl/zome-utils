[package]
name = "zome_utils"
version = "0.0.136"
edition = "2021"

[lib]
name = "zome_utils"
crate-type = [ "cdylib", "rlib" ]

[dependencies]
serde = { version = "1.0", default-features = false }

hdk = { version = "=0.0.134", default-features = false }
holo_hash = { version = "=0.0.26", default-features = false, features = ["encoding"] }
